package _250702;

import java.util.Arrays;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;

public class 대충_만든_자판 {

	public static void main(String[] args) {
		String[] keymap = {"ABACD", "BCEFD"}; String[] targets= {"ABCD","AABB"};
//		String[] keymap = {"AA"}; String[] targets= {"B"};
//		String[] keymap = {}; String[] targets= {};
		int[] answer =  new int[keymap.length];
		
		Map<Character, Integer> map = new HashMap<>();
		Map<Character, Integer> Link = new LinkedHashMap<>();
		
		for(int i =0; i<keymap.length; i++) {
			for(int j =0; j<keymap[i].length(); j++) {
				Link.put(keymap[i].charAt(j), j+1);
			}
		}
		for(int i =0; i<keymap.length; i++) {
			for(int j =0; j<keymap[i].length(); j++) {
				
				
					map.put(keymap[i].charAt(j), j+1);
				
				
			}
			
			
			 System.out.println( "Link:"  +Link);
			System.out.println( "map:"  +map);
			
		}
	}

}
