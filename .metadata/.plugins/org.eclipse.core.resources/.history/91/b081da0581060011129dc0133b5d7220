package algorithm.programmers.Lv0;

import java.util.Arrays;

public class 배열_조각하기 {

	public static void main(String[] args) {
		int[] arr = {0,1,2,3,4,5};
		int[] query = {4,1,2};
		int[] answer = {};
		for (int i = 0; i < query.length; i++) {
			for (int j = 0; j < arr.length; j++) {
				if(query[i] % 2 == 0) {
					answer =  Arrays.copyOfRange(answer, 0, query[i]);
				}else{
					answer =  Arrays.copyOfRange(answer, query[i], answer.length);
				}
			}
		}
		
//		int[] answer = Arrays.copyOf(arr, arr.length-1);
//		
//		for (int i = 0; i < query.length; i++) {
//			if(query[i] % 2 == 0) {
//				answer =  Arrays.copyOfRange(answer, 0, query[i]+1);
//			}else{
//				answer =  Arrays.copyOfRange(answer, query[i], answer.length-1);
//			}
//
//		}
//		System.out.println(Arrays.toString(answer));
		
		
	      
	}

} 
