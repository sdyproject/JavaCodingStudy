package _250424;

import java.util.ArrayList;
import java.util.Arrays;

public class 문자열_잘라서_정렬하기 {
	public static void main(String[] args) {
		String myString = "axbxcxdx";
//		String myString = "xabcxdefx";
//		String myString ="dxccxbbbxaaaa";
		//세번째 방식 풀이
		/* 1. myString.split("x")를 통해 x를 기준으로 잘라 sarr배열에 넣는다.
		 * 두번째 방식 풀이와 다르게 고정 크기를 가지지않는 배열리스트를 통해 
		 * for문으로 sarr배열에서 공백이 아닐 시 배열리스트에 값을 넣는다.
		 * 2. 공백이 아닌 값들이 들어간 배열리스트 slist에 길이(size())를 String배열 answer 길이로 고정
		 * 3. slist.toArray(answer);를 통해 배열리스트를 배열로 변환
		 * 4. Arrays.sort(answer)를 통해 오름차순 정렬 
		 * 5. 풀이 성공
		 * */
		String[] sarr = myString.split("x");
		
		ArrayList<String> slist = new ArrayList<>();
		
		for(String str : sarr) {
			if(!str.isEmpty()) {
				slist.add(str);
			}
		}
		
		String[] answer = new String[slist.size()];
		slist.toArray(answer);
		Arrays.sort(answer);
		 System.out.println(Arrays.toString(answer)); //1.  2번 출력 : [abc, def] 3번 출력 :[aaaa, bbb, cc, d] 
		
		//두번째 방식 풀이 
		/* 1. myString.split("x")를 통해 x를 기준으로 잘라 arr배열에 넣는다. 출력값 : [, abc, def] 
		 * 2. 공백이 발생 
		 * 3. String[] answer 배열의 크기를 arr의 길이로 맞춰 진행했지만 문제 발생 
		 *  문제 발생 원인
		 *  배열은 크기가 고정적이기 때문에 1번을 통해 발생된 공백 또한 출력값에 들어가기 때문에 
		 *  문제 조건에 맞지 않음
		 *  
		 * 	 String[] arr = myString.split("x");
			 String[] answer = new String[arr.length];
			 for(int i =0; i<answer.length; i++) {
				if(!arr[i].isEmpty())
					answer[i] = arr[i];
			 }
			
			 System.out.println(Arrays.toString(answer));
		 * */


		 
		 
		 
		 //기존 내가 푼 풀이 
		 
		/* 문제점 
		 * myString으로 주어진 문자열이 "xabcxdefx"이라면 빈 공백이 배열에 포함될 수 있다.(문제 조건에 공백은 포함되면 안된다는 조건 존재)
		 * 결과값이 [, abc, def] 나오게 된다.
		 * 
		 *  String[] answer = myString.split("x");
			Arrays.sort(answer);
			System.out.println(Arrays.toString(answer)); 
			System.out.println(answer.length);
		 */
		 
		
	}
	
}
