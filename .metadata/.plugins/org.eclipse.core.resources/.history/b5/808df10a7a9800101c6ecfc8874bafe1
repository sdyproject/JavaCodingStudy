package 스택_큐_덱_1;

import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.util.Arrays;
import java.util.Deque;
import java.util.LinkedList;
import java.util.Queue;
import java.util.StringTokenizer;

public class queuestack {

	public static void main(String[] args) throws IOException {
		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
		StringBuilder sb = new StringBuilder();
		int N = Integer.parseInt(br.readLine());

		Deque<Integer> q = new LinkedList<>();
		int[] arr = new int[N];
		int[] arr2 = new int[N];
		StringTokenizer st = new StringTokenizer(br.readLine());
		StringTokenizer st2 = new StringTokenizer(br.readLine());

		for (int i = 0; i < N; i++) {
			arr[i] = Integer.parseInt(st.nextToken());
			arr2[i] = Integer.parseInt(st2.nextToken());
		}

		int M = Integer.parseInt(br.readLine());
		int[] arr3 = new int[M];
		StringTokenizer st3 = new StringTokenizer(br.readLine());

		for (int i = 0; i < M; i++) {

			arr[i] = Integer.parseInt(st3.nextToken());
		}

		for (int i = 0; i < arr.length; i++) {
			if (arr[i] == 0) {
				q.add(arr2[i]);

			}

		}
		System.out.println(q);

		
//		for (int i = 0; i < M; i++) {
//			q.addFirst(arr[i]);
//			
//		}
//		System.out.println(q);

		/*
		 * 문제 해석 큐스택이라는 자료구조를 만들려고한다. 첫번째 줄 큐스택을 구성하는 자료구조의 개수 N 두번째 줄 (0 1 1 0) 0일땐 큐
		 * 1이라면 스택 세번째 줄 (1 2 3 4) 큐(1) 스택(2) 스택(3) 큐(4) *각각의 자료구조에는 한 개의 원소가 들어있다.
		 * 
		 * 네번째 줄 큐스택에 삽일한 수열 M 주어짐
		 * 
		 * 다섯번째 줄 큐스택에 삽입될 원소를 담은 길이의 M 수열이 주어짐
		 * 
		 * 데이터 삽입 과정 1. x 입력 (2 4 7) 2. 1번 자료구조 *큐 에 x 삽입 후 원소 pop하고 그것이 x1 x1==1 3. x1를
		 * 2번 자료구조 *스택에 삽입 후 원소 pop하고 그것이 x2 x2==3 ... Xn의 값을 출력
		 * 
		 * 삽입후 pop하는것이기 때문에 스택은 데이터가 변하지않음 들어왔던 데이터가 바로 pop된다.
		 * 
		 * 
		 * 
		 * 
		 * 
		 */
	}

}
