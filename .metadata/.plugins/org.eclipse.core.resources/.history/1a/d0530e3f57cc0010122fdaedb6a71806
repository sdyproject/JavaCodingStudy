package algorithm.programmers.lv1;

import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.io.Reader;

import java.nio.Buffer;
import java.time.LocalTime;
import java.util.StringTokenizer;

public class PCCP_기출문제1번_동영상_재생기 {

	public static void main(String[] args) throws IOException{
		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
		StringTokenizer st = new StringTokenizer(br.readLine());
		
		String video_len = st.nextToken(); // 동영상 길이
		String pos = st.nextToken(); // 기능 수행 전 위치
//		String op_start = st.nextToken(); // 오프닝 시작 시간을 나타내는 위치
//		String op_end = st.nextToken(); // 오프닝이 끝나는 위치
//		String commands =  br.readLine(); // 명령 : prev 10초전으로 이동,  next 10초 후로 이동 만약 pos가 오프닝 시간이라면 자동으로 op_end으로 이동
//		String[] arr = commands.split(" "); // 명령어 배열
//		
		System.out.println(video_len );
		System.out.println(pos);
	}

}
